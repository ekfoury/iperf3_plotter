# Legacy shell implementation

This directory preserves the original `jq` + `gnuplot` implementation from
`iperf3_plotter` v1.

The project root now contains compatibility wrappers that call the Python v2
CLI. Use the legacy scripts only when you need to reproduce the old behavior
exactly.

