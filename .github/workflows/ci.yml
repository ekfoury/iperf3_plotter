name: CI

on:
  push:
  pull_request:

jobs:
  test:
    name: Python ${{ matrix.python-version }} on Ubuntu
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.10"
          - "3.11"
          - "3.12"

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package
        run: |
          python -m pip install --upgrade pip
          python -m pip install .

      - name: Run unit tests
        run: python -m unittest discover -s tests

      - name: Run sample analysis
        run: |
          iperfplot all sample/my_test.json --out /tmp/iperfplot-sample --format png
          test -f /tmp/iperfplot-sample/report.html
          test -f /tmp/iperfplot-sample/data/flow_intervals.csv
          test -f /tmp/iperfplot-sample/data/experiment_summary.csv
          test -f /tmp/iperfplot-sample/plots/throughput_flows.png

      - name: Run custom plot spec
        run: |
          iperfplot custom sample/my_test.json --plot-spec examples/custom_plots.yaml --out /tmp/iperfplot-custom --format png
          test -f /tmp/iperfplot-custom/rtt_cdf_by_flow.png

      - name: Run experiment file
        run: |
          iperfplot validate examples/experiment.yaml
          iperfplot experiment examples/experiment.yaml --out /tmp/iperfplot-experiment --format png
          test -f /tmp/iperfplot-experiment/report.html
          test -f /tmp/iperfplot-experiment/plots/sample_throughput.png
